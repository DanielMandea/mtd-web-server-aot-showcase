<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
        <h1>AOT Smoothies 🍹 list</h1>
        #for(item in items):
        <div>
            <h2>#(item.title)</h2>
            <p>#(item.description)</p>
            <p>#(item.ingredinets)</p>
            <p>#(item.calories)</p>
        </div>
        #endfor
    <meta name="description" content="#(index.description)">
  </head>
  <body>
      
    <main>
        #import("body")
    </main>
  </body>
</html>
